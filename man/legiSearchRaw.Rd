% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/legiSearchRaw.R
\name{legiSearchRaw}
\alias{legiSearchRaw}
\title{Return raw search results from legiscan's database}
\usage{
legiSearchRaw(
  query = NULL,
  state = "ALL",
  year = 2,
  sessionID = NULL,
  page = 1,
  maxPages = 10,
  legiKey = NULL
)
}
\arguments{
\item{query}{Enter your search text, at most 1024 bytes (UTF-8; accented
characters count as 2 or more). Your query may use grammatically correct spacing. Use legiscan's search syntax for more powerful results.}

\item{state}{Search the entire nation by default with 'ALL' or specify state
using a two letter abbreviation, 'DC', or 'US' for Congress (case-insensitive).
Ignored when `sessionID` is supplied}

\item{year}{Should be an integer. 1=All, 2=Current, 3=Recent, 4=Prior,
>1900=Exact Year. Ignored when `sessionID` is supplied}

\item{sessionID}{Limit search to specific session with a session_id}

\item{page}{Default is set to return Page 1. `legiSearchRaw` will paginate and include results.}

\item{maxPages}{Maximum number of pages to fetch. Each page is one API query
against your monthly quota. Use `Inf` to fetch every page.}

\item{legiKey}{32 character string provided by legiscan}
}
\value{
A data frame (tibble) of search results combined across fetched
pages, with \code{relevance}, \code{bill_id}, and \code{change_hash}. When no results are
found, a warning is issued and a zero-row tibble with these columns is returned.
}
\description{
Return search results from legiscan's full text engine with simplified details,
2000 results at a time. Returns relevance, bill_id, and change_hash only,
appropriate for automated keyword monitoring. Check legiscan.com for specific search syntax for more details.
}
\examples{
\dontrun{
legiSearchRaw(
  query = "intro:month AND wage theft",
  state = "TX"
)
}

}
